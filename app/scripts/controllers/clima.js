'use strict';

/**
 * @ngdoc function
 * @name usaidMockupsApp.controller:ClimaCtrl
 * @description
 * # ClimaCtrl
 * Controller of the usaidMockupsApp
 */
angular.module('usaidMockupsApp')
  .controller('ClimaCtrl', function ($scope) {
    $scope.items = [];
    /*cultivoFactory.listar().then(function (data) {
      $scope.items = data;
    });*/
  });
